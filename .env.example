# Optional: Add a GitHub Personal Access Token to increase API rate limits
# from 60 req/h (unauthenticated) to 5000 req/h
# Create at: https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=
